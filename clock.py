# Import the datetime library
from datetime import datetime

# Create a variable called current and assign it the value
# of the function now() from the datetime library
current = datetime.now()

# Display only a part of the current time on each line
print current.year
print current.month
print current.day
